
import type { ReactNode } from 'react';
import PublicLayout from '@/app/(public)/layout';

export default function TestimonialsPageLayout({ children }: { children: ReactNode }) {
  return <PublicLayout>{children}</PublicLayout>;
}
