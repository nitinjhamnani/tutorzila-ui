
import HomePage from "@/app/(public)/page";

// The root page now simply renders the public homepage.
// The authentication and redirection logic is handled by the layout.
export default function RootPage() {
  return <HomePage />;
}
